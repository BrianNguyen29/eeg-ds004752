model_id: privileged_a4
status: smoke_revision_non_claim
revision_note: "A bounded A4 privileged train-time-only smoke is implemented for post-prereg engineering validation only. The smoke uses an internal training-subject privileged/teacher-side proxy to validate split discipline, gate/weight handling, privileged-output handling, student fitting and scalp-only inference. It is not the final A4 privileged-transfer comparator and cannot support decoder efficacy, A4 efficacy or privileged-transfer claims."
claim_scope: non_claim_implementation_smoke_only
final_comparator_ready: false
smoke_config: configs/phase1/a4_smoke.json
leakage_guards:
  normalization_fit_scope: training_subjects_only
  gate_weight_fit_scope: training_subjects_only
  privileged_proxy_fit_scope: training_subjects_only
  privileged_outputs_for_student_fit_scope: training_subjects_only
  student_fit_scope: training_subjects_only
  inference_privileged_outputs_allowed: false
  inference_scalp_only_required: true
  outer_test_subject_in_any_fit_allowed: false
